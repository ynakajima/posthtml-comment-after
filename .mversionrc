{
  "commitMessage": "chore(ver): v%s",
  "scripts": {
    "postcommit": "git checkout master && git merge develop && git push --all && git push --tags && git checkout develop",
    "precommit": "conventional-changelog -p angular -i CHANGELOG.md -s && git add ."
  }
}
